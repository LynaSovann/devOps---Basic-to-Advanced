# This contains the essential commands to use with the Vagrant

## Initialization

- Creates a new `Vagrantfile` in the current directory.

```bash
vagrant init
```

- Creates a Vagrantfile using a specific base box.

```bash
vagrant init <box-name>
```
